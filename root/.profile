#!/bin/sh

. /opt/etc/profile

# Aliases
alias grep='grep --colour=auto'
alias ls='ls --color=auto'
alias cdz='cd /i-data/.system/zy-pkgs'

# Set colors to match iTerm2 Terminal Colors
export TERM=xterm-256color

# Default locale settings
export LANG=ru
export LC_ALL=ru_UA.UTF-8

